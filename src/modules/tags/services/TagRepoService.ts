/*  Controller
 * vai criar criar uma tag em um repository
 *  vai receber o id do repository(github) por parametro
 *  vai receber o nome da tag por body
 * Create Tag service:
 *  vai verificar se a tag existe no banco de dados;
 *  se nao exitir a tag, criar ela;
 * TagRepoService
 *  criar uma entidade associativa tags_repositories
 */
